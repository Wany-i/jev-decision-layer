# 安全策略

## 报告漏洞

**不要开公开 issue。**

请用 GitHub 的私密渠道：
→ https://github.com/Wany-i/jev-decision-layer/security/advisories/new

如果能附上最小复现、影响范围、以及你建议的修法，会快很多。

**我会尽力在 7 天内回复。** 这个项目是单人在业余维护，做不到更快的承诺，但每一条都会看。

---

## 这个项目的信任面（请先读，能省你很多时间）

### 密钥

- 本仓库**任何文件都不含密钥**。运行时从环境变量 `OPENROUTER_API_KEY` 读取。
- 密钥只出现在发往后端的 `Authorization: Bearer` 头里，**不会被写盘、不会被日志打印**。
- 如果你的 key 泄露了：**立刻去控制台吊销并换新** —— 它进过 git 历史的话，删文件是没用的，必须轮换。

### `state` 是数据，不是指令 —— 这是最需要防的一类问题

决策模型的 `state` 里装的是**不可信内容**（用户消息、网页文本、商品标题、文档片段）。
底层模型**不会把 state 当敌意内容处理** —— 写在 state 里的指令、误导性框定、甚至"为自己的分类辩护"的文字，都可能推动答案。

**这意味着**：这个项目从设计上就**不适合**直接把不可信文本喂进去、再把结果当成安全判据去执行高风险动作。如果你要那样用：

1. 在 `criteria` 里把条件写死、写精确
2. 给高危动作加 `guards`，并在代码里再设一道独立校验
3. 先做影子测试，再上生产

### 这个项目**不是**安全边界

- 它不做鉴权、不做配额、不做审计
- 它不保证答案正确 —— 门控（`gate`）只表达"模型有多确定"，不表达"这个判断对不对"
- 置信度会随 `state` 形状漂移（实测同一判断可偏 ±0.33），所以**阈值必须在你自己数据上标定**

**请把决策结果当作"一条建议"，而不是"一个已授权的动作"。** 副作用、幂等、回滚、审批都在你自己的代码里。

---

## 支持范围

| 版本 | 是否接受安全修复 |
|---|---|
| 最新 release | ✅ |
| `main` 分支 | ✅ |
| 更早的 release | ❌ 请先升级 |
