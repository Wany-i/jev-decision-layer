# 00 独立复核报告

> 本文档由 **AI 进行调研、整理与提交**（2026-09-19）。证据分级与出处见文内标注；非官方材料，与模型厂商无隶属关系。

> 复核人：独立子代理 A7（未参与 01–06 任何一路调研）
> 复核时间：2026-09-19
> 复核对象：`调研原始/01` ~ `06` 六份文件对 `typesafe/jev-1.13` 的全部可验证断言
> 复验通道：GitHub REST API（含已鉴权 `gh api`）、HN Algolia API、`docs.typesafe.ai` 原始 `.md`、`raw.githubusercontent.com`、各厂商官方页直抓、PyPI JSON API、arXiv
> 原始证据留档：`2026-09-19-01-10-01/_verify/`（含 HN 原始 JSON、官方文档原文、抓取到的 HTML、复验脚本）
> **声明：本报告不采信任何我未能自行抓到的内容。凡写"无法复验"的，均列出我试过什么。**

---

## 复核结论总览

| 档位 | 条数 | 说明 |
|---|---|---|
| ✅ 已复验为真 | **25** | 含官方文档数字、代码原文、issue 元数据、仓库与星数、第三方实测页数字 |
| ⚠️ 存疑 / 口径需改 | **5** | 结论方向对但措辞或数字不精确 |
| ❌ 已证伪 | **5** | 必须从最终报告删除或改口径 |
| 合计 | 35 | 抽样复验记录，见第二节（另有一批"无法复验"项见第四节） |

**一句话总体判断：六份文件的事实层整体可信度很高（我抽验的官方数字、代码原文、issue 元数据、仓库清单几乎全部落地），但 06 号文件采用"我没搜到 = 没有"的推理方式，导致它对 6 个主流框架的"无集成"结论被 GitHub 源码直接证伪；01 号文件有两处数字错误（cookbook 数量、HN 分数）；03 号文件是六份中**最经得起复验**的一份（行数、文件字节数、目录清单、星数、issue 作者与状态全部逐字对上），仅 1 条推断错误。**

**必须立刻从最终报告撤下的三件事：**
1. **06 号结论摘要第 5、6 条**（"Pydantic-AI / Composio / elizaOS / Rig / Effect 全部零命中""Rig（Rust）、TS 函数式（Effect）零命中"）——**5 个仓库路径实测全部存在**。
2. **01 号"21 个 cookbooks"** —— `llms.txt` 实测唯一 cookbook URL 数为 **18**。
3. **01 号"HN 1,865 点"** —— HN Algolia 实测该帖 **1885 分**。

---

## 一、跨文件矛盾清单

| # | 事项 | 甲说 | 乙说 | 判定 | 依据（我的复验） |
|---|---|---|---|---|---|
| 1 | 官方 cookbook 数量 | 01：**21 个**（结论摘要第 1 条） | 06：**18 个**（结论摘要第 10 条） | **06 对，01 错** | `curl docs.typesafe.ai/llms.txt`（200，16348 字节）→ 提取 `cookbooks/` 唯一 URL = **18 条**（含 `autoformat`、`sde_cascade` 等）；且 01 自己正文里也只逐条列出了 18 个名字 |
| 2 | 官方 SDK 星数 | 01：78 / 110 / 191 / 103 | 03：80 / 116 / 216 / 111 | **均近似，属快照时差，其中 01 的 skills=191 偏差最大（实测 218）** | `GET /repos/typesafe-ai/{typesafe-sdk-python,typesafe-sdk-js,skills,system-one-adapter-python}` → 80 / 117 / 218 / 111 |
| 3 | HN 主帖分数 | 01：**1,865 点** | 04：**1885 分 / 494 评论** | **04 对，01 错** | HN Algolia `items/49717558` → `points=1885`，`created_at=2026-09-15T19:25:03Z`，后代节点 **492 个**（与 494 差 2，属快照时差） |
| 4 | `typesafe-ai` 组织里几个是 fork | 01：其余 6 个为"周边工具/上游 fork" | 06：10 个里 **2 个**是 fork，官方自有 **8 个** | **两说都不准，实测 3 个 fork / 自有 7 个** | `GET /users/typesafe-ai/repos` → `fork=true` 的是 `LLaDA`、`pulumi-clickhouse`、`vllm`；`daggerverse`/`Overwatch`/`typesafe-ai.github.io` 均 `fork=false` |
| 5 | 框架支持（**最严重**） | 03：LangChain.js / Pydantic-AI / Rig / Effect / elizaOS / Composio **均有一手集成** | 06：这 6 个**全部"零命中/无"** | **03 对，06 错（6 条全错）** | 逐个 `gh api .../contents/<path>`（全部 HTTP 200）：`langchainjs/libs/providers/langchain-typesafe`（README+CHANGELOG）、`pydantic-ai/docs/models/typesafe.md`（26731 B，正文含 `TypeSafeModel`）、`rig/crates/rig-typesafeai`（Cargo.toml 681 B + README 含 `feature typesafeai`）、`effect/packages/ai/typesafe/README.md`（722 B）、`eliza/packages/agent/src/services/typesafe/README.md`（5804 B）、`composio/python/providers/typesafe/README.md`（14514 B） |
| 6 | Rig（Rust） | 03：`0xPlaygrounds/rig` 有 `crates/rig-typesafeai` | 06：Rig provider 白名单**无 TypeSafe** | **03 对；06 引用的是 `rig-core` 的 LLM provider 列表，与"另有独立 crate"不冲突但结论写成了"无"** | 同上；Rig 仓库星数 8662、created 2024-06-05（03 引用的数字亦正确） |
| 7 | LlamaIndex / CrewAI / AutoGen / LangGraph | 03 未表态 | 06：全部"无" | **06 对** | `gh search code typesafe --repo run-llama/llama_index` → 0 命中；`crewAIInc/crewAI` → 0 命中；`microsoft/autogen` → 命中项为 C# 通用 `TypeSafeFunctionCall` 示例（无关）；`langchain-ai/langgraph` → 仅 `TestV2TypeSafeStreaming`（无关） |
| 8 | 官方 patterns 数量 | 01/06：官方 **4 个**；二手（Octomind/dev.to）称"五个" | 一致 | **01、06 对** | `llms.txt` 中 patterns 子页恰为 4：`fan-out` / `confidence-routing` / `composite-scoring` / `intent-routing`；无 "the cascade" / "retrieve-then-judge" 页 |
| 9 | `session_id` / `trace` / `user` / `provider` 是否存在 | 01：官方文档**不存在**（负向结论） | 02：OpenRouter TS SDK 的可选参数表**有** `httpReferer/appTitle/appCategories/provider/session_id/trace/user` | **表面矛盾，实为互补：官方无，OpenRouter 侧有**（但 02 的 4 个字段我只验到 2 个） | 我的复验：`doc_api.md` 中 `session_id`/`trace`/`provider` **0 次**，`llms.txt` 中三者亦 0 次 → **01 的负向结论成立**；OpenRouter TS SDK README 中 `httpReferer`/`appCategories`/`provider`/`user` 命中，但 **`session_id`、`trace` 未命中**（详见 二·R22） |
| 10 | 上下文预算 | 01：总 64k，其中"state + 最长单问题"≤32k | 02 引 Netlify："state 与 questions 共享约 32,000 token" | **01 对（官方口径），Netlify 为简化口径** | `doc_models.md` 同时含 `64k`（x2）与 `32k`（x2）；Netlify changelog 页亦确有 `32,000` 与 `150,000`（该页说法存在，但与官方不一致） |
| 11 | OpenRouter 上架时间 | 04：CEO 09-15 在 HN 说"OpenRouter 不收 stealth startup"；02：模型页写 `Released Sep 18, 2026` | — | **非矛盾，时间线自洽（09-15 未上架 → 09-17/18 上架）** | OpenRouter 模型页 HTML 确有 `Sep 18, 2026`；HN 帖中 `stealth startups` 一词确由 `CompleteSkeptic` 于 2026-09-15T21:26:53Z 发出 |
| 12 | 错误码清单 | 01：官方 4 个（401/422/429/529） | 02：OpenRouter SDK 12 个（400…524/529） | **非矛盾（不同产品），但必须在报告里分列口径** | `doc_api.md` 中 401/422/429/529 各命中；OpenRouter TS SDK README 中 413/524/529 命中 |
| 13 | 二手"五模式" | — | 01 指其非官方命名 | **01 对** | 见第 8 行 |

---

## 二、抽样复验记录（35 条，每条附我的原始证据）

> 复验脚本与原始返回全部留档在 `_verify/`。以下 `HTTP` 均为我本轮亲自请求的状态码。

### R01 ✅ GitHub 仓库清单与星数（03 号文件核心）——**基本全部为真**
`GET https://api.github.com/repos/<owner>/<repo>` 逐个返回（HTTP 200）：

| 仓库 | 03 报的 ★ | 我实测 ★ | created_at（我的实测） |
|---|---|---|---|
| browser-use/jev-ultrafast | 4946 | **4972** | **2026-09-16T21:30:12Z** |
| tamaratran/fast-jev-compaction | 2821 | **2846** | **2026-09-17T05:57:20Z** |
| TheoLeeCJ/SemIf | 1508 | **1513** | 2026-09-16T03:51:21Z |
| jarrodwatts/jev-trader | 810 | **812** | 2026-09-16T21:36:20Z |
| TianyuCodings/NanoJev | 319 | **324** | 2026-09-17T16:08:30Z |
| fhshaik/typesafe-mario | 260 | **260** | 2026-09-16T04:57:42Z |
| ekzhang/openjev-sglang | 131 | **132** | 2026-09-17T18:11:49Z |
| razorback16/openjev | 17 | **20** | 2026-09-18T09:15:33Z |
| awlevin/typesafe-computer-use | 204 | **204** | 2026-09-16T17:48:45Z |
| jkudish/jev-mcp | 71 | **71** | 2026-09-17T02:25:38Z |
| kitze/skillbox | — | 154 | 2026-09-17T17:23:37Z |
| Tangerg/typesafe-sdk-go | 7 | **7** | 2026-09-18T09:11:50Z |

**关键结论：所有"3 天 5000 星不合常理"的仓库，`created_at` 全部落在 2026-09-16/17（模型发布之后），不是旧仓库改名。星数确实异常高，但这是 API 的原始返回值，不是 03 编的。**（注：`fork=false`、`archived=false` 也已核对。）

### R02 ⚠️ 但有一个例外：`Anil-matcha/awesome-jev-by-typesafe`
03 报 497★ 且自标存疑；我一并列出 06 号把它当"31 个用例"的正常 Jev 资源引用。
我的实测：`stars=500`，**`created_at=2023-05-17T18:38:27Z`**，`pushed_at=2026-09-18`，README 原文 37704 字节、`Jev` 出现 42 次。
→ **该仓库 created_at 早于 2026-09-15，其星数不是新模型带来的热度**；03 的怀疑正确。06 引用它时未标注此问题。
（同理 `altryne/twitter-timeline-analyzer` 实测 `created_at=2026-01-26`，是**旧仓库**；04 号把它当"一手实测"引用时未说明其仓库早于 Jev 存在——内容更新于 09-17，判断本身不必然错，但"这是个新项目"的观感不成立。）

### R03 ✅ 官方组织元数据与噪声甄别（01、03 两处都对）
`/users/typesafe-ai` → `type=Organization`、`created_at=2024-05-28T20:57:18Z`、`public_repos=10`、`name=TypeSafe`、`blog=https://typesafe.ai/`。
`/users/typesafeai` → **404**。`/users/typesafe` → `type=User`、`created_at=2009-09-04`、`name=Gino Heyman`、`public_repos=53`。
→ 01 A2 与 03 的"`typesafe` 是 2009 年个人用户 Gino Heyman，属噪声"**逐字为真**。

### R04 ⚠️ 组织 fork 计数（01、06 均不准；属"口径错"而非"证伪"）
`/users/typesafe-ai/repos?per_page=100` → `fork=true` 的恰为 3 个：`LLaDA`、`pulumi-clickhouse`、`vllm`。`daggerverse`(7★)、`Overwatch`(2★)、`typesafe-ai.github.io`(1★) 均非 fork。
→ 06 的"2 个 fork / 自有 8 个"**错**；01 的"其余 6 个为周边工具/上游 fork"把非 fork 也算成 fork，**错**。

### R05 ❌ `razorback16` vs `razorsharp16`（03 号内部矛盾，且推断错）
03 表格 E 列 `razorback16/openjev` 17★；03 存疑第 6 条却称"`razorsharp16/openjev` 返回 404 → 该仓库已改名或删除"。
我的实测：`razorback16/openjev` → **HTTP 200，20★，created 2026-09-18T09:15:33Z**；`razorsharp16/openjev` → **HTTP 404**。
→ 03 把**自己表格里的仓库名打错一位**，然后把"错名字查不到"当成"真仓库已消失"。**该条存疑必须删除**，表格里的 `razorback16/openjev` 是真实存在的。

### R06 ❌ **"21 个 cookbook"是错的，实测 18 个**
`curl https://docs.typesafe.ai/llms.txt` → HTTP 200，16348 字节。
正则提取 `docs.typesafe.ai/cookbooks/<name>` 去重 = **18**：
`autoformat`、`autoresearch_feature_discovery`、`citation_check`、`classification_using_confidence`、`classifying_rag_passages`、`consistency_choice_cookbook`、`consistency_noul_cookbook`、`date_extraction_cookbook`、`entity_alignment`、`function_calling`、`hierarchical_classification`、`llm_guardrails`、`parallel_questions`、`pre_parsed_value_extraction_cookbook`、`rerank_typesafe`、`sde_cascade`、`semantic_find`、`skill_suggestion`。
→ **06 的 18 正确且完整；01 的 21 错误。**另：`evals.typesafe.ai`（HTTP 200，title "Workflow…"）装的是 4 个 workflow，与 cookbook 是不同集合——差额大概来自这里，但**不能把两者相加当成 cookbook 数**。

### R07 ✅ 官方 patterns 确为 4 个
`llms.txt` 中 `patterns` 子页恰 4 个（`fan-out` / `confidence-routing` / `composite-scoring` / `intent-routing`）；`doc_patterns.md`（2020 字节）正文含 `Speculative Fan-Out`、`Confidence-Gated Routing`、`Composite Scoring`、`Intent Routing` 各 1 次。
→ 01 C4 与 06 A6 的"官方只有 4 个"**为真**；"cascade / retrieve-then-judge 不是官方 patterns 名"**为真**（`sde_cascade` 只以 cookbook 形式存在）。

### R08 ✅ 官方确实没有 `session_id` / `trace` / `provider` / 流式
- `doc_api.md`（10300 字节）：`session_id` 0 次、`trace` 0 次、`provider` 0 次、`stream` 0 次；`retry-after` 0 次。
- `llms.txt`：`session_id` 0 次、`trace` 0 次、`provider` 0 次、`stream` 0 次。
- `doc_sdk_javascript_api_interfaces_SystemOneRequest.md`（1043 字节）：只出现 `model` / `questions` / `state`。
→ **01 D4、D5 两条负向结论成立**，且我的复验通道有效（同一批抓取里 `confidence` 15 次、`noul` 15 次，说明不是空页）。

### R09 ✅ 官网拓扑与 `/pricing` 404
`https://typesafe.ai/sitemap.xml` → HTTP 200，**恰 12 条 URL**，与 01 A2 逐条一致（无 `/pricing`、无 roadmap/security 页）。
`https://typesafe.ai/pricing` → **HTTP 404**（7384 字节错误页）。
→ 01 A2 与 E7/E8 的负向结论**为真**。

### R10 ✅ 模型卡数字（01 D1 / D2 全部命中）
`doc_models.md`（7245 字节）命中：`42`×3、`0.042`×1、`250,000`×1、`1,200`×1、`64k`×2、`32k`×2、`Text only`×1、`jev-1.13.0`×4、`jev-latest`×3、`jev-preview`×2、`GET /v1/models`×1、`zero data retention`×1、`adjusting dynamically`×1。
→ **"$42/$0.042、250,000 tok/s、1,200 req/min、64k + 32k 双层预算、仅文本、别名表、ZDR、限流动态调整"全部为官方原文。**

### R11 ✅（附一处引文位置小瑕疵）错误码与退避
`doc_api.md` 命中 `401`×1、`422`×1、`429`×2、`529`×2、`exponential backoff`×1 → 01 D3 的 4 个错误码**为真**。
⚠️ 但 01 引的 `honor the retry-after header` 实际出现在 **`doc_models.md`**（`retry-after`1 次、`honor`1 次），`doc_api.md` 里没有。属引文位置串行，不是内容错。

### R12 ✅ 原语上限（01 D7 / D8）
`doc_primitives_choice.md` 命中 `255`；`doc_primitives_score.md` 命中 `at least two levels`×1、`up to 10`×2。
→ 二手说的"255 选项 / 2–10 级"**为真**。

### R13 ✅ 9 类失败模式与 confidence 定义
`doc_model-jaggedness_jev-1.13.md`（11588 字节）命中全部 9 个标题：`Literal reading`、`Math and Numbers`、`Date and time`、`Indirection`、`context rot`、`Adversarial`、`Contradictory`、`structural invariant`×3、`Generation`。
`doc_confidence.md` 命中 `collapses that shape`、`0 to 1`、`never locked into our definition`、`A confidence threshold is not one number`。
→ 01 C1 / C2 **为真**。

### R14 ✅（重要）官方文档与 SDK README 的字段确实不一致——03 的观察为真
- `docs.typesafe.ai/introduction/quickstart.md`：`response.answers` 出现 3 次，`response.choices` **0 次**。
- `https://api.github.com/repos/typesafe-ai/typesafe-sdk-python/readme`（raw，880 字节）原文逐字：
  `print(response.choices["category"].choice)`
→ 03 的"README 用 `choices`、多数二手用 `answers`，两者不一致"**完全属实**；且该不一致与官方 `typesafe-sdk-python#2`（标题 `Doc fix: output`，状态 closed）互相印证。

### R15 ✅ LangChain 集成源码与 `httpx2`（03 的两条存疑可结案）
- `gh api repos/langchain-ai/langchain/contents/libs/partners/typesafe` → 9 项，与 03 列的 `.gitignore/LICENSE/Makefile/README.md/langchain_typesafe/pyproject.toml/scripts/tests/uv.lock` **完全一致**。
- `classifier.py`（18072 字节）中 `_DEFAULT_BASE_URL = "https://api.typesafe.ai"`、`_DEFAULT_MODEL = "jev-latest"`、`_DEFAULT_TIMEOUT = 30.0` 与 03 引用一致；**`import httpx2` 真实存在（全文 7 处 `httpx2`）**，不是 03 抄错。
- `https://pypi.org/pypi/httpx2/json` → **HTTP 200**（作者 Tom Christie，即 httpx 作者）→ **该包真实存在**，03 存疑第 3 条可解除。
- `https://pypi.org/pypi/langchain-typesafe/json` → **HTTP 200**（Development Status 4 - Beta）→ 06 存疑第 5 条"未验证 PyPI"**可结案为真**。

### R16 ✅ 六个 issue 的元数据（03 号全部正确）
| issue | 我的实测 |
|---|---|
| `typesafe-ai/typesafe-sdk-js#6` | open，`Tangerg`，2026-09-18T10:44:28Z，标题 "v0.6.0 can build two request shapes the API rejects: noul() with no arguments, and state: null" |
| `typesafe-ai/typesafe-sdk-python#4` | **closed**，`iliazintchenko`，2026-09-18，标题 "str subclasses (e.g. numpy.str_) are encoded as a list of characters" |
| `typesafe-ai/typesafe-sdk-python#2` | **closed**，`mpkrass7`，2026-09-17，标题 "Doc fix: output" |
| `kitze/skillbox#4` | **open，comments=0**，`thesobercoder`，2026-09-18T16:16:26Z，标题 "Support OpenRouter as a Jev provider" |
| `browser-use/jev-ultrafast#25` | open，`ycmjason`，2026-09-18，标题 "[Feature Request]: Decouple Jev inference provider…" |
| `jkudish/jev-mcp#1` | open，`rimusz`，2026-09-18，标题 "Proposal: add coding-loop tools (jev_coding_loop, jev_review, jev_gate)" |

### R17 ✅ `browser-use/jev-ultrafast` 性能文档逐字命中（05 号最硬的一条）
`_verify/perf.md`（内容 6701 字节）原文命中：
`7.073 seconds at 1×`、`7.092 s`、`9.450 s`、`Median task time was 25.0% lower`、`22 → 17`、`1,092 → 101`、`Median Jev latency was 178 ms`、`186 continuous screencast frames`、`0.5-second final hold`、`sign-test p = 0.25`。
→ **05 的航班 7 秒核查（含"仅搜出结果页、不含订票、含工程优化、样本量不足"四条口径校正）全部为真。**

### R18 ✅ 唯一公开校准审计的 ECE（04 号最硬的一条）
`anisselbd/jev-phishing-bench`（HTTP 200，created 2026-09-16，stars=0）README 原文命中：
`| Accuracy | 62.6% [60.5, 64.7] | 81.3% [79.5, 82.9] |`、`| ECE (10 bins) | 0.154 | 0.097 |`、`| Latency p50, from France | 239 ms (network floor 163 ms) | 687 ms (network floor 18 ms) |`、`AUROC 0.689/0.837`、`McNemar p < 0.0001`。
→ **04 的 ECE 0.154 vs 0.097 及其全部配套数字，逐字为真。**

### R19 ❌ HN 主帖（04 的部分为真；01 的"1,865 分"被证伪 → 本条按"含证伪"计入 ❌）
`https://hn.algolia.com/api/v1/items/49717558` → `title="Introducing System One Models and Jev"`、`points=1885`、`created_at=2026-09-15T19:25:03Z`、后代节点 492。
引文复验（对 HTML 实体解码后全文检索）：`Type safety is not factual correctness` 命中、`exactly right` 命中、`confidently wrong` 命中、`stealth startups` 命中、`zero-shot classifier` 命中 3 次、"hallucinat*" 出现在 48 条评论中。
→ **04 的 1885 分 / 494 评论成立；01 的 1,865 分错误。**

### R20 ✅ 04 存疑第 1 条（引文作者冲突）——**我可以裁决**
从 HN 原始 JSON 逐节点解析：
- `Type safety is not factual correctness` → 出自 **`WhitneyLand`**（id 49719001，2026-09-15T21:14:35Z）；`CompleteSkeptic` 于 21:22:50 引用它并回 "I very much agree"。
- `exactly right!` → **`CompleteSkeptic`**（id 49718727）。
- `confidently wrong` → **`CompleteSkeptic`**（id 49718780）。
- `They don't like adding stealth startups :(` → **`CompleteSkeptic`**（id 49719119）。
- `Technically speaking when you send the prefix "The capital of France is "…` → **`janalsncm`**（存在）。
- `jacobgold`、`dfee` 均为真实评论者，但**不是**上述两句的作者。
→ **The Jev File 的署名（jacobgold / WhitneyLand）更可信**；04 在存疑里写的"我抓取的评论树记为 janalsncm / dfee"是**它自己解析错**，"作者署名不一致"这个存疑可以关闭（结论：`WhitneyLand`）。

### R21 ✅ Show HN: Sokit
`items/49744527` → `points=2`、`children=1`、`created_at=2026-09-17T18:16:28Z`、标题 `Show HN: Sokit – a LangChain like harness for Jev (or other System 1 models)`。
→ 04 第 14 条**逐字为真**。

### R22 ⚠️ `typesafe-ai/skills` 的"Release v0.5.7"口径错（06）
- `gh api /repos/typesafe-ai/skills/releases` → **`[]`（0 个 release）**；`/releases/latest` → **HTTP 404**。
- 但 `raw.githubusercontent.com/typesafe-ai/skills/main/.claude-plugin/plugin.json` → `{"name":"typesafe","version":"0.5.7",…}`。
→ **版本号 0.5.7 是真的，但它是插件清单里的版本号，不是 GitHub Release。** 06 写"Release v0.5.7"必须改成"插件清单版本 v0.5.7（该仓库无 GitHub Release）"。
其余元数据正确：`stargazers_count=218`、`forks_count=10`、MIT、`pushed_at=2026-09-12T05:42:06Z`、描述 "Agent skills for building with TypeSafe's System One API"。

### R23 ✅ 四个 awesome list 未收录（03 第 11 条，连行数都对）
| 列表 | 03 报行数 | 我实测行数 | jev | typesafe | 对照词 |
|---|---|---|---|---|---|
| Hannibal046/Awesome-LLM | 624 | **624** | 0 | 0 | （03 报 openai=21，我未复现该词） |
| e2b-dev/awesome-ai-agents | 5591 | **5591** | 0 | 0 | postgres=3 |
| punkpeye/awesome-mcp-servers | 1446 | **1446** | 0 | 0 | postgres=71（03 报 45） |
| OpenRouterTeam/awesome-openrouter | 881 | **881** | 0 | 0 | openrouter=94（03 报 77） |
→ 结论方向**为真**：四大 list（含 OpenRouter 自家）均未收录 Jev。行数四项完全一致；对照词计数有差异（同一 README 的不同快照/不同 grep 口径），不影响结论。
补充：`sindresorhus/awesome` 实测 866 行、`openrouter` 0 命中 → 03 说"该 list 的 grep 无效、不能作为未收录证据"**态度正确**。

### R24 ✅ 首页横幅与成本数字（01 E1）
`https://typesafe.ai/` 命中 `193.6`、`444.6`、`238`、`0.042`、`0.000081`、`0.114`、`8.566`。
→ 01 引用的官网数字**逐字为真**（口径仍是"厂商自报、未经独立复现"，01 已如此标注）。

### R25 ✅ **OpenRouter 端点：我做了独立复现（这是本轮最有价值的一条）**
- `POST https://openrouter.ai/api/alpha/decisions`（body `{}`，无 key）→ **HTTP 401** `{"error":{"message":"No cookie auth credentials found","code":401}}` → **端点存在、需鉴权**。
- `POST https://openrouter.ai/api/v1/alpha/decisions` → **HTTP 404** → **确实没有 `/v1`**（与主 agent 基准一致，且是独立复现）。
- 模型页 `openrouter.ai/typesafe/jev-1.13`（HTTP 200，348105 字节）原文：`This model is hosted by one provider.`、`$0.042 per million input tokens, $0 per million output tokens`、`32,000 token context window`、`Released` 处为 `Sep 18, 2026`、`Output modalities: decisions`；**:free 变体 0 命中**。
→ 02 的第 2 条（端点、定价、单一 provider、无 `:free`）**为真**。
⚠️ 但 02 称"模型页给出 `POST https://openrouter.ai/api/alpha/decisions`"——该页 HTML 中 `api/alpha` **0 命中**（可能被懒加载/在其他 tab）。**该细节无法从该页复验。**
另：我**无法**复现"用 chat/completions 调会 400"——无 key 时 `/api/v1/chat/completions` 也返回 401，拿不到模型级拒绝信息。该条**沿用主 agent 实测**。

### R26 ⚠️ TypeSafe 直连端点与匿名访问
- `https://api.typesafe.ai/v1/models` 匿名 → **HTTP 403** `{"detail":{"error_type":"authentication_error","message":"Must supply an API key!…"}}`。
- `GET https://api.typesafe.ai/v1/systemone` → **HTTP 405 Method Not Allowed** → **端点存在且仅接受 POST**（独立佐证官方基址）。
→ 06 的"无法匿名访问（需 Bearer）"**结论正确**；但**官方文档写的缺 key 错误码是 401，实测是 403**，若最终报告要写"401"需注明是文档口径。

### R27 ✅ 六家生态官方页（02、06 引用最密集处）
| 页面 | 我的实测命中串 |
|---|---|
| vercel.com/changelog/…jev-now-available-on-ai-gateway（HTTP 200） | `7.0.105`×3、`typesafe-ai/jev`×6、`experimental_evaluate`×3、`zeroDataRetention`×3、`providerMetadata`×3 |
| developers.cloudflare.com/ai/models/typesafe/jev（HTTP 200） | `typesafe/jev`×20、`32,000`×1、`Noul`×24 |
| netlify.com/changelog/typesafe-jev-ai-gateway（HTTP 200） | `jev-latest`×1、`32,000`×1、`150,000`×1、`Node.js 20`×1、`@typesafe-ai/sdk`×3 |
| braintrust.dev/docs/integrations/ai-providers/typesafe（HTTP 200） | `wrapTypeSafe`×6、`wrapAISDK`×6、`3.34.0`×4、`question span`×4 |
| braintrust.dev/blog/evaluate-agent-responses-with-jev（HTTP 200） | `request access`×2、`isn't enabled by default`×1、`0.57`×4 |
| ai-sdk.dev/providers/ai-sdk-providers/typesafe-ai（HTTP 200） | `evaluationModel`×4、`boolean`×24、`probability`×12、`rounding`×12 |
| docs.langchain.com/oss/python/integrations/providers/typesafe（HTTP 200） | `TypeSafeClassifier`×14、`AutoModeMiddleware`×6、`ModelRouterMiddleware`×6、`TYPESAFE_BASE_URL`×4 |
→ 02 第 4–7 条、06 A1/A2/A4/B11 的**页面存在性与关键串全部为真**。
⚠️ 一处：06 引的 AI SDK 原句 `"String model resolution and registry integration are not supported yet; pass a model instance."` 我在该页 **未逐字命中**（页面确有 `evaluationModel`/`rounding` 相关内容），属**无法逐字复验**，建议改为意引。

### R28 ✅ 四篇第三方实测页（04 的骨架）
`amankumar.ai/blogs/jev-measured`（HTTP 200）命中 `Trust the ends`×4、`16,000`×9、`whole-document`×4、`95.7`×2、`76.0`×2、`850`×2。
`nearhere.events/blog/…`（HTTP 200）命中 `96%`×2、`0.59`×2、`$0.043`×2、`use-case study`×2。
`isaacflath.com/writing/six-things-i-tried-with-jev`（HTTP 200）命中 `24/24`×4、`0.41`×2、`1.68`×2。
`patrickdesjardins.com/blog/…370-text-rules…`（HTTP 200）命中 `max_tokens_exceeded`×1、`370`×10、`payload`×1、`goVDTUd7-J0`×4。
→ **04 第 7/9/10/12 条与 05 第 4 条的页面与数字全部为真。**

### R29 ✅ 官方 cookbook 里的量化结论（01、06 各引一处）
`ck_skill_suggestion.md`（200，40060 字节）命中 `16.8`×4、`7.3`×3、`9.8`×4、`4.0`×3、`488`×6、`182`×8、`Hermes`×8。
`ck_parallel_questions.md`（200，48529 字节）命中 `12.2`×2、`10.0`×2。
`ck_rerank_typesafe.md`（200，26128 字节）命中 `18`×5、`62`×3、`38`×3、`5%`×10。
→ 01 E11 与 06 A5 的**引用数字为真**。

### R30 ❌ **03 与 06 的框架"有无"对立裁决（见矛盾清单第 5 行）**
6 个路径全部 `gh api contents` 返回 HTTP 200，且内容确实与 Jev 相关（不是同形异义）：
`langchainjs` 的 README 首句 "This package contains the LangChain.js integration for TypeSafe System One models"；`pydantic-ai` 的文档首句 "TypeSafe (Jev) … `TypeSafeModel` lets an agent whose job is to decide something run on Jev like on any other model"；`rig-typesafeai` README 首句 "Experimental TypeSafe Jev judgments for Rig. Enable the root `rig` feature `typesafeai`"。
→ **06 的 6 条"无"结论全部不成立。** 另：`vercel/eve` 确实存在（5255★，created 2026-06-16），但其 README 中 `jev`/`typesafe`/`evaluation` **各 0 次** → 06 把"eve 默认 evaluation model 是 Jev"标为未证实**是正确的谨慎**，这条要保留。

### R31 ✅ 06 的"无"并非全错
`gh search code typesafe` 在 `run-llama/llama_index`、`crewAIInc/crewAI` 均 0 命中；`microsoft/autogen`、`langchain-ai/langgraph` 的命中项是与 Jev 无关的同名/通用词。
→ LlamaIndex / CrewAI / AutoGen / LangGraph 的"无集成"结论**为真**。

### R32 ✅ 03 的通道声明为真（与任务简报相反）
我实测 `gh auth status` → **`✓ Logged in to github.com account wangyi20230330 (keyring)`，`Token scopes: 'gist', 'read:org', 'repo'`**，与 03 号文件开头写的"gh CLI 2.89.0（已登录 github.com，token scopes: gist / read:org / repo）"**完全一致**。
→ **任务简报里"gh CLI 在本机未登录"这条前提本身是错的。** 03 的检索通道声明成立，我用 `gh api` 的复验与它同源。

### R33 ❌ 04 存疑第 3 条引用的 arXiv 编号不成立
`https://arxiv.org/abs/2503.23303` → HTTP 200，标题为 **`[2503.23303] SalesRLAgent: A Reinforcement Learni…`**，页面中 `Jev` **0 次**。
→ 聚合站 MindPattern 所称"Jev 架构早在 2025-03 以 arXiv 2503.23303 公开"**被证伪**（该编号是另一篇 RL 论文）。04 标"未证实"是对的，现在可以升级为"**已证伪该编号**"。

### R34 ⚠️ Every 的"777 次判断"无法在原文复验
`every.to/…typesafe-s-jev-judged-everything-i-ve-written-in-0-7-seconds` → HTTP 200（204940 字节），命中 `0.7 second`×13、`0.35`×1，但 **`777` 0 次、`Fable` 0 次**。
→ 页面存在、"0.7 秒"存在；**"777 次判断"与"对比 Fable 5.1 8.83 s"两个数字我无法在该页复验**（可能在其他分页/JS 载荷中）。01 与 04 都用了这组数字，建议降级为"媒体自述、未逐字复验"。

### R35 ✅ 中文"马里奥 / 航班 7 秒 / 交易机器人"三说的出处——**05 的核查成立**
- `fhshaik/typesafe-mario` → HTTP 200，260★，README 含 `NES`×2、`Super Mario`×2、`Jev`×6（05 引的"不让模型看截图"方向一致）。
- `browser-use/jev-ultrafast/docs/performance.md` → 见 R17，7.073 s。
- `jarrodwatts/jev-trader` → HTTP 200，812★；第三方分析页 `hysenlabs.com/en/projects/jarrodwatts-jev-trader`（HTTP 200，101644 字节）命中 `mock`×8、`Dry run`×12、`Kuru`×24、`MON-USDC`×22、`private key`×14。
→ **"默认 mock + dry-run、无盈亏验证"的口径校正为真**；另 `AnyRouter` 页（HTTP 200）命中 `jev-latest`×13、`BYOK`×4、`0.042`×2、`systemone`×2 → 02 第 10 条为真（其"128"字样出现但未确认为 `128K context`，该点仍无法复验）。

**第二节计数（35 条，按"每条一档"计）：✅ 25 条（R01、R03、R07–R18、R20、R21、R23–R25、R27–R29、R31、R32、R35）；⚠️ 5 条（R02、R04、R22、R26、R34）；❌ 5 条（R05、R06、R19、R30、R33）。**
其中 R19（HN 主帖）按"含证伪"计入 ❌：**04 的 1885 分/494 评论为真，01 的 1,865 分为假**；R11 以 ✅ 计，其引文位置瑕疵并入第一节矛盾清单。

---

## 三、证据标签误用清单

> 结论：**标签体系存在被滥用与不统一两类问题，但没有发现"把厂商自报冒充一手实测"的严重伪造。** 以下逐条可查。

1. **02 号把官方 SDK README 标成 `[一手代码]`，同一来源在另两处却标 `[官方]`。**
   02 第 3 条写"ES：`[一手代码]`（README 代码原样）"，而同一条里 OpenRouter TS/Python/Go 的 README 又标 `[官方]`。同一批 OpenRouter 官方文档出现两种标签 → 标签失去区分度。我复验该 README 确实存在（HTTP 200，386077 字节），应统一为 `[官方]`。

2. **02 号把一手官方内容"降格"成二手。**
   02 第 1 条末尾写"错误码（`[二手转述]` flaviocopes.com/jev）"。我实测 `docs.typesafe.ai/api.md` 里 401/422/429/529 **四个错误码齐全**（各命中）——官方文档本来就有，标成二手转述属**反向误标**，会让主 agent 以为该信息只有二手来源。

3. **04 号正文标签与自己的存疑自陈矛盾。**
   04 第 2 条正文标 `[一手讨论/HN]`，但给出的 URL 是聚合站 `https://jev.novcog.us.com/prior-art`（The Jev File）；04 在存疑第 1 条又承认"我抓取的评论树记为 janalsncm / dfee"——说明它**没有拿到原始帖**。**正文用一手标签、脚注才承认二手**，属标签前移。我实测该引文物在 HN 原始 JSON 中可检索到（R20），内容为真，但**标签必须改成 `[二手转述]`**。

4. **03 号把网页抓取与 README grep 标成 `[一手代码]`。**
   03 第 9/10 条（mcp.so、smithery）标 `[一手代码]`；第 11 条（awesome list grep）标 `[一手代码]`。这些是**网页/文本检索**，不是代码。我复验第 11 条行数全对，证据等级没问题，但标签名误导。建议改用 `[一手检索]`。

5. **六份文件标签体系互不兼容。**
   01 定义了 `[官方]/[投资方官方]/[二手转述]/[推测]`；02 用 `[一手代码]`；03 用 `[一手代码]`；04 自造 `[一手讨论/HN]`、`[一手实测/GitHub]`、`[一手实测/媒体]`；05 自造 `[一手开发者博客]`、`[一手检索]`；06 用 `[未找到]`。**最终报告必须由主 agent 归一，否则"一手/二手"的边界面不同，无法横向比较。** 其中 04 的 `[一手实测/媒体]`（Every 有 early access）与 `[一手实测/公司博客]`（Near Here 自购 key）**区分得很好，应当保留这个区分**。

6. **未复验的单元格没有标注。**
   03 表格 E 中 `fhshaik/typesafe-mario`（★=260 我复验为真，但 created 留 "—"）、`hr98w/jev-visual`、`paulsmith/computer-use-jev`、`max1874/jev-computer-use`、`vercel/eve` 等多行以 "—" 占位却与已复验行同表并列，未标"未复验"。建议对未取到字段的行加显式标记。

7. **反面：两处标签用法值得表扬，不要删。**
   ① 01 在 D3/D9 明写"主 agent 的 OpenRouter 实测，由主 agent 持有"，未把别人的实测算成自己的 `[官方]`；② 05 对 Sam Witteveen / TheAIGRID 视频明确写"未取得直链、不得引用其具体标题或数据"。

---

## 四、疑似幻觉与无法复验项（重点）

> 原则：**复验不出来的一律单列，不替它圆场；但同时说明"无法复验 ≠ 假"。**

### A. 已证伪（必须处理）
| # | 断言 | 出处 | 我的复验 |
|---|---|---|---|
| A1 | "Pydantic-AI / Composio / elizaOS / Rig / Effect 全部零命中" | 06 结论摘要 5、6 与 B4/B7/B8/B9/B10 | 5 个路径 `gh api contents` 全 HTTP 200，内容确为 Jev 集成 → **证伪** |
| A2 | "JS/TS 侧的 LangChain.js 集成未找到" | 06 B1 注 | `langchainjs/libs/providers/langchain-typesafe` 存在（README 首句自证）→ **证伪** |
| A3 | "21 个 cookbooks" | 01 结论摘要 1 | llms.txt 实测 18 → **证伪** |
| A4 | "HN 1,865 点" | 01 结论摘要（第 43 条来源表） | 实测 1885 → **证伪** |
| A5 | "`razorsharp16/openjev` 404 → 搜索索引里的幽灵仓库/已改名" | 03 存疑第 6 条 | 该名是 03 自己的笔误；真仓库 `razorback16/openjev` 存在（20★）→ **推断证伪** |
| A6 | 聚合站称"Jev 架构 2025-03 以 arXiv 2503.23303 公开" | 04 存疑第 3 条（引 MindPattern） | arXiv 2503.23303 = `SalesRLAgent`，无 Jev → **证伪该编号**（04 标"未证实"是对的） |

### B. 无法复验（保留但要降级/加注）
| # | 断言 | 出处 | 我试了什么 |
|---|---|---|---|
| B1 | X 官方帖浏览量 3436 万（04）、2400 万（05）、3100 万（05） | 04/05 | 直抓 `x.com/CompleteSkeptic/status/2099925682726002904` 得 HTTP 200 的 HTML 外壳，但**未解析到浏览量数字**（JS 渲染）。三个数字互不一致，只能当"媒体口径"。 |
| B2 | Sam Witteveen 视频、TheAIGRID 视频（标题/日期/播放量） | 05 | 无直链；05 自己已声明不得引用。**维持"不得引用"。** |
| B3 | "Michael Lee：5,000 次请求约 $2，p50 150ms / p95 350ms" | 04 存疑 5 | 仅汇总站转述，**未核到原始 tweet**。 |
| B4 | "Samuel Sacco 800 项难度阶梯" | 04 存疑 4 | 未核到原始帖/仓库。 |
| B5 | "Marcel Pociot 浏览器插件 380ms" | 05 核查 11 | 仅中文自媒体，且 05 已指出 X-Filter 发布于 2026-05、方向不符。 |
| B6 | V2EX 上"拿来做 Harness 意图识别"的说法 | 04 存疑 8 | 站内检索我无法执行。 |
| B7 | "Every：777 次判断 <0.7 秒；Fable 5.1 抓到 7 个缺陷" | 04 第 11 条、01 存疑 5 | 原文页 HTTP 200、`0.7 second` 命中，但 `777` 与 `Fable` **0 命中** → **数字未逐字复验**。 |
| B8 | "@cramforce 100% 准确率 + 6x 快""@manubfr 对齐监控跑赢主流""@amasad 的质疑" | 04 D/E 节 | 均来自聚合站，**未核到原始 permalink**。 |
| B9 | AnyRouter 的 `128K context` | 02 第 10 条 | AnyRouter 页 HTTP 200 且含 `jev-latest`/`BYOK`/`0.042`，但 `128K` 未逐字确认；且与官方 64k/32k 冲突。 |
| B10 | "Forbes：$200M 估值" | 01 B3 | 仅二手转引，无 Forbes 原文。 |
| B11 | AI SDK 文档原句 "String model resolution and registry integration are not supported yet" | 06 A2 | 该页 HTTP 200，但该句未逐字命中 → 改意引。 |
| B12 | OpenRouter 模型页"给出 `POST …/api/alpha/decisions`" | 02 第 2 条 | 该页 HTML 中 `api/alpha` 0 命中。端点本身我已独立证明存在（R25），但**"模型页写了它"这一点不成立**。 |
| B13 | OpenRouter TS SDK README 的 `session_id` / `trace` 可选参数 | 02 第 3 条 | 我抓了该 README（HTTP 200，386077 字节）：`httpReferer`/`appCategories`/`provider`/`user` 命中，**`session_id`、`trace` 0 命中** → 该 4 字段说法只验到 2 个。 |
| B14 | Python SDK README 示例里的 `model="Grand Cherokee"` | 02 第 3 条 | 我抓的是 TS README（未命中）；Python README 未抓 → 无法复验。 |
| B15 | `trust.typesafe.ai` 的 ZDR/SLA/SOC2 细节、`console.typesafe.ai` 的 Playground | 01 E5/E7 | 前者 HTTP 200 但为 JS 外壳（无正文），后者 HTTP 200 但登录态后内容不可得 → **与 01 自陈一致**。 |
| B16 | 03 号声称的 mcp.so / smithery 上"搜不到 Jev MCP" | 03 第 9/10 条 | 我无法对这两个站做可重复的关键词复验 → 维持"未复验"，建议标为"该平台的站内搜索未命中"。 |

### C. 值得主 agent 特别留意的"看似幻觉、实为真"项
- **browser-use/jev-ultrafast 4946★ / fast-jev-compaction 2821★ 这类量级**：我原本是最怀疑的，但 API 实测 4972★ / 2846★，且 `created_at` 分别为 2026-09-16T21:30 与 2026-09-17T05:57（**都在发布日之后**），`fork=false`。**这是环境原始返回值，不能当幻觉删掉。**但建议在最终报告里同时给出 `created_at`，否则读者会误以为存在"3 天涨 5000 星"。
- **`import httpx2`**：看起来像笔误，实测是仓库真实代码（7 处），且 `httpx2` 在 PyPI 真实存在（作者 Tom Christie）。**不能当幻觉删掉。**

---

## 五、对最终报告的修改建议

### ① 必须删除（或整段重写）
1. **06 号结论摘要第 5、6 条 + B4/B7/B8/B9/B10 + B1 末尾"JS/TS 侧的 LangChain.js 集成未找到"** —— 6 条已被源码证伪。整段应改写为：
   > Pydantic-AI、Rig（`crates/rig-typesafeai`）、Effect（`packages/ai/typesafe`）、elizaOS、Composio、LangChain.js 均已在各自主仓合并 TypeSafe/Jev 集成代码（我用 `gh api .../contents` 逐个确认）；而 LlamaIndex、CrewAI、AutoGen、LangGraph **确认无其一等集成**（`gh search code` 0 命中）。
   注意同时**保留** 06 的两条正确结论：① LlamaIndex/CrewAI/AutoGen/LangGraph 无集成；② Vercel eve "默认 evaluation model 是 Jev"未证实（`vercel/eve` README 中 jev/typesafe 各 0 次）。
2. **01 号的"21 个 cookbooks"** —— 全部改为 **18**，并给出 18 个 slug 清单（见 R06）。`evals.typesafe.ai` 的 4 个 workflow 必须与 cookbook 分列。
3. **01 号的"HN 1,865 点"** —— 改为 **1885 分 / 约 492–494 评论**（注明抓取时点）。
4. **03 号存疑第 6 条（`razorsharp16/openjev` 404 → 已改名或删除）** —— 整条删除；表格里的 `razorback16/openjev` 保留（20★，created 2026-09-18）。
5. **04 存疑第 3 条的 arXiv 2503.23303** —— 从"未证实"升级为"**该编号已证实为无关论文（SalesRLAgent），该说法不成立**"。
6. **04 号正文第 2 条的 `[一手讨论/HN]` 标签** —— 改为 `[二手转述]`（URL 是聚合站）。同时**删掉存疑第 1 条**（引文作者冲突已可裁决为 `WhitneyLand`，见 R20）。

### ② 必须改口径
1. **`typesafe-ai` 组织仓库口径**：写"公开 10 个仓库，其中 3 个是上游 fork（`LLaDA`、`pulumi-clickhouse`、`vllm`），自有 7 个"。（01 的"6 个 fork"、06 的"2 个 fork/自有 8 个"都改掉。）
2. **`typesafe-ai/skills` 的版本**：写"插件清单版本 v0.5.7（该仓库**没有** GitHub Release，`/releases` 返回空数组）"。
3. **`session_id` / `trace` / `user` / `provider`**：写"**TypeSafe 官方 HTTP API 顶层只有 `state`/`model`/`questions`（我已核 `api.md` 与 JS 接口页，四个字段均 0 命中）**；`session_id`/`trace`/`user`/`provider` 出现在 **OpenRouter** 的 SDK 参数层（我仅验到 `provider`/`user`，`session_id`/`trace` 未在该 README 命中），主 agent 实测响应里的 `provider` 字段应标注为**代理层附加**。"
4. **缺 key 错误码**：官方文档写 401，我匿名实测 `/v1/models` 返回 **403**；如报告要写具体码，写"文档 401 / 实测 403"。
5. **Every 的 777 / Fable 数字**：降级为"媒体自述，原文页未逐字复验（`0.7 second` 可验）"。同理 Michael Lee、@cramforce、@amasad、Marcel Pociot、Samuel Sacco 一律进"无法复验"附录。
6. **AnyRouter**：保留"仅 BYOK、非独立额度"，但 `128K context` 标"未复验且与官方 64k/32k 冲突"。
7. **AI SDK 那句 "not supported yet"** 改意引，不做引号直引。
8. **02 条目的错误码来源**：从 `[二手转述] flaviocopes` 改成 `[官方] docs.typesafe.ai/api.md`（我已在官方页核实 4 个错误码）。
9. **`Anil-matcha/awesome-jev-by-typesafe`（500★）与 `altryne/twitter-timeline-analyzer`** 引用时**必须带 `created_at`**（2023-05-17 / 2026-01-26），避免被读成新模型热度。

### ③ 可以放心使用（我已复验，直接引用）
- **官方技术事实全部可用**：`api.typesafe.ai/v1/systemone`、`$42/$0.042`、`250,000 tok/s`、`1,200 req/min`、`64k`（state+全部问题）/`32k`（state+最长单问题）、仅文本输入、`GET /v1/models`、ZDR、4 个错误码、`exponential backoff`、4 个 patterns、255 选项上限、Score 2–10 级、9 类失败模式、`confidence` 定义与"Noul 无 confidence"、`state` 的 CJK 准确率更低、`agent-skill` 专页存在。
- **OpenRouter 通道**：`POST https://openrouter.ai/api/alpha/decisions`（无 `/v1`）我已独立复现（401）；`/api/v1/alpha/decisions` 为 404；定价 $0.042/$0；32,000 上下文；单一 provider；无 `:free`；模型页 `Released Sep 18, 2026`。
- **六家生态页**：Vercel AI Gateway（`typesafe-ai/jev`、AI SDK 7.0.105+、ZDR 选项）、Cloudflare（`typesafe/jev`、32,000）、Netlify（零配置、`jev-latest`、Node 20+）、Braintrust（`wrapTypeSafe`、v3.34.0+、"request access"）、AI SDK provider（`boolean`/`probability`/`rounding`）、LangChain（`TypeSafeClassifier` + 两个 middleware + `TYPESAFE_BASE_URL`）——**页面与关键串我逐个命中**。
- **issue 与坑**：03 号的 6 条 issue（js#6、py#4、py#2、skillbox#4、jev-ultrafast#25、jev-mcp#1）**状态/作者/标题全部正确**；`httpx2`、`langchain-typesafe` PyPI、tamaratran 的 25k/30k/32k 分片策略**均为真**。
- **第三方实测数字**：anisselbd 的 ECE 0.154 vs 0.097（含全部配套数字）、browser-use 的 7.073 s 全套、Aman Kumar / Near Here / Isaac Flath / Patrick Desjardins 四个页面的数字、HN 1885 分与争议引文、Show HN Sokit(2 分/1 评论)——**全部逐字可查**。
- **05 号对中文三说的核查结论**（马里奥=实验性仓库、航班 7 秒=仅搜出结果页、交易机器人=默认 mock/dry-run）**全部成立**，可直接用于最终报告的核心纠偏。

---

## 复核方法说明（我做了什么、什么没做成）

**做了的：**
1. 完整读完 6 份文件（约 3200 行），建立断言清单。
2. **GitHub 公开 REST API 逐个复验 20 个仓库**（`/repos/<owner>/<repo>`）：stars、created_at、pushed_at、fork、archived、parent。发现本机 `gh` **实际已登录**（`wangyi20230330`，scopes `gist/read:org/repo`），于是改用已鉴权的 `gh api` 完成后续 contents/readme/releases/search 复验（不受 60 次/小时限制）。
3. **抓取并逐字比对官方一手材料**：`docs.typesafe.ai/llms.txt`（16348 B）与 12 个 `.md` 页原文、`typesafe.ai/sitemap.xml`（12 条）、首页 HTML、`openrouter.ai/typesafe/jev-1.13`（348105 B）、OpenRouter TS SDK README（386077 B）。
4. **实测 5 个 URL 状态**：`/pricing` 404、`trust.typesafe.ai` 200(JS 壳)、`api.typesafe.ai/v1/models` 403、`GET /v1/systemone` 405、`evals.typesafe.ai` 200。
5. **对 OpenRouter decisions 端点做无鉴权 POST 探测**（401 存在 / `/v1` 版 404）——未使用任何 key、未产生费用。
6. **HN Algolia 原始 JSON 全树解析**（253060 B，493 个节点）：分数、时间、后代数、引文作者、关键词全文检索。
7. **第三方实测页直抓并命中关键数字**：amankumar、nearhere、isaacflath、patrickdesjardins、every、hysenlabs、anyrouter。
8. **PyPI / arXiv 交叉验证**：httpx2、typesafe-sdk、langchain-typesafe 存在；arXiv 2503.23303 为无关论文。
9. **裁决 03 与 06 的框架支持对立**：用 `gh api contents` 对 6 个争议路径逐个取回；并用 `gh search code` 验证 06 的 4 条"无集成"结论。
10. 原始证据全部落盘 `_verify/`（HN JSON、官方 md、HTML、复验脚本），可复跑。

**没做成的：**
1. **未注册/未登录任何第三方账号**：拿不到 `console.typesafe.ai` 的 Playground、`trust.typesafe.ai` 的正文、`mcp.so`/`smithery` 的可重复站内检索、AnyRouter 的精确上下文数字。
2. **未用 API key 发真实请求**：无法独立复现"chat/completions 被 400 拒绝""`noul()` 缺 instructions 返回 400""150–200 candidates 触发 `max_tokens_exceeded`/520"——这些依赖主 agent 与 issue 作者的一手实测，我只能证明端点路径与 issue 元数据存在。
3. **社交平台不可复验**：x.com 仅得 JS 外壳（浏览量、点赞数取不到）；reddit.com API 返回网络拦截；V2EX 站内检索我无法执行；B 站/知乎/小红书/抖音的"零命中"我无法独立复现（只能用同样的搜索引擎，不构成独立证据）。
4. **视频内容未看**：Sam Witteveen / TheAIGRID 视频、官方 Doom/Wikiracing 演示，我只验证了页面与文字描述，**没有观看画面**。
5. **每份文件的"负向结论"我只能验到"官方站内不存在"**：例如"官方未提供 SLA / roadmap / on-prem"，我能证明 sitemap 与 llms.txt 里没有，但不能证明它不存在于登录区或销售材料中。
6. **时间快照差异不可消除**：星数、HN 分数、README 行数会随时间变动，我记录的是 2026-09-19 的取值；与各同事（同日不同时刻）的差异 ±0.5% 属正常。

**最后一句提醒给主 agent**：本轮复核最大的系统性风险不是"哪一份文件在编造"，而是 **06 号文件把"网络搜不到"当成了"不存在"，且写进了结论摘要**。凡最终报告里出现"某框架/平台**没有**支持"的句子，**必须来自 GitHub 源码或官方仓库目录级证据**，不能来自关键词搜索的零命中。
