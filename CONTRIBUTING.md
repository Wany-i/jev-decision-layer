# 贡献指南

感谢你来。这个项目的目标很窄：**把决策模型包成可以直接用在业务里的决策工具**。所以贡献也围绕这一件事。

---

## 最快的贡献方式：提一个新决策

不用改代码，在 `registry/` 加一个 JSON 就行。

```json
{
  "name": "你的决策名",
  "description": "一句话说清它判什么",
  "version": "0.1.0",
  "primary": "主问题名",
  "state_fields": ["字段1", "字段2"],
  "default_threshold": 0.7,
  "thresholds": { "选项A": 0.85, "选项B": 0.7 },
  "guards": [],
  "questions": {
    "主问题名": {
      "type": "choice",
      "instructions": "按什么口径判",
      "criteria": { "选项A": "说明A", "选项B": "说明B", "other": "都不合适" }
    }
  }
}
```

### 提之前请自查这几条

| 自查项 | 为什么 |
|---|---|
| 答案空间**能提前穷举** | 开放式判断应该用生成模型，不是这个 |
| 是**语义判断**，不是算术/计数/日期比较 | 官方明说它不是计算器，且计数不可靠、误差随规模增长 |
| `state_fields` 只放**真正需要**的字段 | 实测：同一判断加入 26k tokens 无关内容，置信度可偏 ±0.33。**多塞字段不是省事，是在毁结果** |
| `choice` 的 criteria **带 `other`** | 否则模型只能在给定选项里挑个最不坏的 |
| 阈值**按动作分别定** | "加价"错了只是少赚，"暂停"错了直接丢流量 —— 不该共用一个数 |
| 高危动作写了 `guards` | 有些判断不能只看置信度。例如"是否不可逆 > 0.3 就转人工" |
| 每份注册表都过 `python -m unittest discover -s tests` | CI 会跑，本地先跑 |

---

## 代码贡献

### 环境

只需 Python 3.10+，**无第三方依赖**。

```bash
git clone https://github.com/Wany-i/jev-decision-layer.git
cd jev-decision-layer
python -m unittest discover -s tests -v      # 28 项，不需要 API key、不联网
```

### 分支与 PR

1. Fork，从 `main` 切一个分支：`feat/<决策名>` 或 `fix/<简述>`
2. 改动 + 补测试
3. 推上来开 PR，**把 PR 模板填完**（尤其"验收证据"那一栏，贴测试结果）

### 代码纪律

- **默认安全**：会写盘/会删东西的入口默认 dry-run，要显式 flag 才真动手
- **不静默降级**：缺字段、后端非 200、注册表不合法 —— 一律显式抛 `DecisionError`，不准吞异常
- **不打印密钥**：任何日志、报错、示例里都不许出现真实 key
- **门控只允许收紧**：`guards` 只能把 `auto` 推成 `review`，反向放宽一律拒
- 改了门控 / 裁剪 / 硬约束 → **必须**补对应单元测试

### 什么会被拒

- 把算术、计数、日期比较塞给模型
- 为了让"结果好看"而放宽阈值或删掉 guards
- 新增第三方依赖去解决标准库能解决的事
- 提交里含真实 key、真实业务数据、或客户可识别信息
- 只写"本地测过了"而没有可复现证据的 PR

---

## 报告问题

- **Bug**：用 Bug 报告模板，给最小复现 + 实际输出
- **新功能**：用功能模板，先说清你的实际场景
- **安全漏洞 / 密钥泄露**：**不要开公开 issue**，见 [SECURITY.md](SECURITY.md)

## 行为准则

参与本项目即表示同意遵守 [行为准则](CODE_OF_CONDUCT.md)。
